# Contributing

欢迎贡献新的 Agent、prompt、brief 模板和导出格式。

## Local Setup

```bash
pnpm install
pnpm generate:demo
pnpm test
```

## Pull Request Checklist

- 新增功能需要包含示例 brief 或测试；
- 输出结构尽量保持 JSON 可解析；
- 不要提交 `.env` 或真实 API Key；
- 不要加入夸大、误导或平台风险表述。
